# not safe in a production environment
bind 0.0.0.0

# working directory
dir ./data

# save the database every 1 second
save 1 1 

# in order to load the ReJSON and Redisearch modules
loadmodule /opt/redis-stack/lib/rejson.so
loadmodule /opt/redis-stack/lib/redisearch.so